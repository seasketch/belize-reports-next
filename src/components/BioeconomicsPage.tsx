import React from "react";
import { MarlinCard } from "./MarlinCard.js";

export const BioeconomicsPage: React.FunctionComponent<{
  printing: boolean;
}> = (props) => {
  return (
    <>
      <MarlinCard />
    </>
  );
};
